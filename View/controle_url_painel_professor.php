<!-- <?php 
   // session_start();

   // $lista_urls = $_SESSION['lista_urls'];

    //$url_request = (isset($_POST["url_request"])) ? $_POST["url_request"] : null ;

    ##################################################
    //$arr_urls_existentes = array();

    //foreach ($lista_urls as $key => $url_existente) 
    {
    //    $arr_urls_existentes[$key] = $url_existente; 
    }
    ##################################################
    // Verifica se a variável (url) é nula
    //if (!is_null($url_request))
    {
        // Verifica se a váriavel (url) já existe na lista (array de urls existentes)
      //  if(!in_array($url_request, $arr_urls_existentes))
        {
            // Se não existir - adiciona a url na lista 
        //    $lista_urls->append($url_request);
        }
    }
    //else
    {
   //     $lista_urls->append($url_control); 
    }
    
   // $i = 0;

    //while($lista_urls->count() > $i)
    {
      //  if($i > 0)
        {
        //    echo(" \ ".$lista_urls[$i]);
        }
        //else
        {
          //  echo($lista_urls[$i]);
        }

        //$i++;
    }

?> -->